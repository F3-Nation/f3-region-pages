# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
env:
  # Configure environment variables.
  # See https://firebase.google.com/docs/app-hosting/configure#user-defined-environment
  - variable: POSTGRES_URL
    secret: postgres-url
    availability:
      - BUILD
      - RUNTIME

  - variable: F3_DATA_WAREHOUSE_URL
    secret: f3-data-warehouse-url
    availability:
      - BUILD
      - RUNTIME

  # Cloud SQL Connector settings (set WAREHOUSE_DB_CONNECTION_MODE=connector to use)
  - variable: WAREHOUSE_DB_CONNECTION_MODE
    value: connector

  - variable: CLOUD_SQL_WAREHOUSE_CONNECTION_NAME
    secret: cloud-sql-warehouse-connection-name
    availability:
      - RUNTIME

  - variable: WAREHOUSE_DB_USER
    secret: warehouse-db-user
    availability:
      - RUNTIME

  - variable: WAREHOUSE_DB_PASSWORD
    secret: warehouse-db-password
    availability:
      - RUNTIME

  - variable: WAREHOUSE_DB_NAME
    secret: warehouse-db-name
    availability:
      - RUNTIME

  - variable: CRON_SECRET
    secret: cron-secret
    availability:
      - RUNTIME

  - variable: SLACK_BOT_AUTH_TOKEN
    secret: slack-bot-auth-token
    availability:
      - RUNTIME

  - variable: SLACK_CHANNEL_ID
    secret: slack-channel-id
    availability:
      - RUNTIME

  # Grant access to secrets in Cloud Secret Manager.
  # See https://firebase.google.com/docs/app-hosting/configure#secret-parameters
  # - variable: MY_SECRET
  #   secret: mySecretRef
