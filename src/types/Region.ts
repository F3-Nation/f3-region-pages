export type Region = {
  id: string;
  name: string;
  description?: string;
  slug: string;
  website?: string;
  image?: string;
  city?: string;
  state?: string;
  zip?: string;
  country?: string;
  latitude?: number;
  longitude?: number;
  zoom?: number;
};
