export type Region = {
  id: string;
  name: string;
  slug: string;
  website?: string;
  city?: string;
  state?: string;
  zip?: string;
  country?: string;
  latitude?: number;
  longitude?: number;
  zoom?: number;
};
